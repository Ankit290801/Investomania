package com.investment.tracker.model;

/**
 * Enum representing expense categories.
 * Categories are configurable from business-rules.yml.
 */
public enum ExpenseCategory {
    HOUSING,
    FOOD,
    TRANSPORT,
    HEALTHCARE,
    ENTERTAINMENT,
    EDUCATION,
    UTILITIES,
    INSURANCE,
    OTHER
}
